node_modules
dist
dist-ssr

# Secrets — never commit
.env
.env.local
.env.*.local

# Plaid connector access tokens (local link state)
server/.tokens.json

# Real business data (keep private; app falls back to bundled sample data)
public/data.json

# Internal working docs (not for the public product repo)
DEPLOY.md
HANDOFF.md
COWORK_SCRAPE_PROMPT.md
MARKET_RESEARCH.md
PRODUCT_PLAN.md
FUNDING_REQUIREMENTS.md
DATA_COLLECTION_CHECKLIST.md

# Strategy / "our edge" — keep private (business plan, leverage playbook,
# demo targets + pricing, data-moat strategy). Never push to the public repo.
HANDOFF_HELM_BUSINESS_PLAN.md
docs/DEMO_ROADMAP.md
docs/HORMOZI_LEVERAGE_CASE_STUDY.md
docs/DATA_CONNECTION_STRATEGY.md

# Internal-only docs (discovery notes, demo runbooks, competitive teardowns). Local, never pushed.
docs/private/

# Misc
.DS_Store
*.log
*.local

# Local dev launcher (not for the repo)
Start Helm.command

# Generated knowledge-graph artifact (rebuild with: graphify update)
graphify-out/
